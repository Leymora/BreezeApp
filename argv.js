var myArgs = process.argv.slice(2);
console.log('myArgs:', myArgs);

switch(myArgs[0])
{
    case '-dab': console.log('sicc dab!'); break;
    case '-ass': console.log('Nikki has the thiccest ass'); break;
}